name: test

channels:
  - conda-forge
  - defaults

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # External packages.
  - mdanalysis >=2.0
  - numpy
  - pint
  - pytorch >=1.9

    # Optional dependencies (here for completeness to test locally).
    # As of Nov 24, 2021, psi4 has a problem with conda-forge. This
    # is a recipe for installing everything that works:
    #       conda create --name tfep python=3.8 psi4 -c psi4
    #       conda activate tfep
    #       conda install mdanalysis">=2.0" pytorch">=1.9" -c conda-forge
    #       pip install --upgrade numpy
    # - psi4 (this can be found on the psi4 conda channel)

    # Pip-only installs
  #- pip:
  #  - codecov

